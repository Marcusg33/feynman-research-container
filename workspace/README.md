# Workspace

This directory is bind-mounted to `/home/agent/workspace` inside the container
and is the only host path the agent can read or write.

Research outputs land here and are immediately visible on the host.
